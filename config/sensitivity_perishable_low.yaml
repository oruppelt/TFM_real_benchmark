# Sensitivity variant: bracket perishable overage cost on the low side (§8.5).
# Inherits from default.yaml; only overrides the perishable regime cost ratios.
extends: default.yaml

family_regimes:
  perishable:
    overage_cost_ratio: 0.6
    underage_cost_ratio: 0.4
